<Window x:Class="MinimalOptimizer2.Views.LanguageSelectionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:svgc="http://sharpvectors.codeplex.com/svgc/"
        Title="Seleção de Idioma" Height="260" Width="420"
        Background="Transparent" WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize" WindowStyle="None" AllowsTransparency="True">
    <Border CornerRadius="10" Background="{StaticResource BackgroundBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Padding="16">
        <Border.Effect>
            <DropShadowEffect Color="Black" BlurRadius="20" ShadowDepth="0" Opacity="0.5"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Text="{DynamicResource Text_SelectLanguage}" FontSize="16" FontWeight="SemiBold" Foreground="{StaticResource TextBrush}" Margin="4,0,0,12"/>
            <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                <Button x:Name="EnglishButton" Width="180" Height="64" Style="{StaticResource PrimaryButtonStyle}" Click="EnglishButton_Click" Margin="0,0,16,0">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                        <svgc:SvgViewbox Width="32" Height="20" Stretch="Fill" Source="pack://application:,,,/Assets/Flags/us.svg"/>
                        <TextBlock Text="{DynamicResource Label_English}" FontSize="14" Margin="10,0,0,0"/>
                    </StackPanel>
                </Button>
                <Button x:Name="PortugueseButton" Width="180" Height="64" Style="{StaticResource PrimaryButtonStyle}" Click="PortugueseButton_Click">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                        <svgc:SvgViewbox Width="32" Height="20" Stretch="Fill" Source="pack://application:,,,/Assets/Flags/br.svg"/>
                        <TextBlock Text="{DynamicResource Label_Portuguese}" FontSize="14" Margin="10,0,0,0"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</Window>